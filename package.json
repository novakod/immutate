{
  "name": "immutate",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "visualize-build": "vite-bundle-visualizer",
    "release": "npm publish --access public"
  },
  "devDependencies": {
    "typescript": "^4.6.4",
    "vite": "^3.2.3",
    "vite-bundle-visualizer": "^0.10.0"
  },
  "dependencies": {
    "@novakod/deep-proxy": "^0.2.1"
  }
}
